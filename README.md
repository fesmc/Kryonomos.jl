# Kryonomos.jl
The law of the cold: stand-alone ice-sheet modeling

### FastIsostasy and Yelmo.jl

To run an example coupled case, use `examples/example_yelmo-fastiso.jl`.

In `examples/`, make sure to add links to locations of `ice_data` and `yelmo:input`.