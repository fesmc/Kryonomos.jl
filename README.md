# Kryonomos.jl
The law of the cold: stand-alone ice-sheet modeling

### FastIsostasy and Yelmo.jl

To run a test case, use `test/test_yelmo-fastiso.jl`. 

In `test/`, make sure to add links to locations of `ice_data` and `yelmo:input`.